import { CalendarPlus, Trash2 } from "lucide-react";
import { createEvent, deleteEvent } from "@/app/admin/actions";

interface WeddingEvent { id: string; title: string; starts_at: string; venue_name: string | null; map_url: string | null; }

export function EventManager({ invitationId, events }: { invitationId: string; events: WeddingEvent[] }) {
  return <section className="mt-14 border-t border-stone-200 pt-10"><div className="flex items-center gap-2 text-rose-700"><CalendarPlus size={19} /><p className="text-sm font-semibold uppercase tracking-[0.18em]">Celebration schedule</p></div><h2 className="mt-3 text-2xl font-semibold">Events your guests can join</h2><p className="mt-2 text-sm text-stone-600">Add Haldi, Mehendi, Reception, or any other celebration. These cards appear on the public invitation.</p><div className="mt-6 grid gap-3">{events.map((event) => <article className="flex items-center justify-between rounded-xl border border-stone-200 bg-white p-4" key={event.id}><div><p className="font-semibold">{event.title}</p><p className="mt-1 text-sm text-stone-500">{new Intl.DateTimeFormat("en-IN", { dateStyle: "medium", timeStyle: "short" }).format(new Date(event.starts_at))}{event.venue_name ? ` · ${event.venue_name}` : ""}</p></div><form action={deleteEvent}><input name="invitationId" type="hidden" value={invitationId} /><input name="eventId" type="hidden" value={event.id} /><button aria-label={`Delete ${event.title}`} className="rounded-lg p-2 text-stone-400 hover:bg-rose-50 hover:text-rose-700"><Trash2 size={17} /></button></form></article>)}</div><form action={createEvent} className="mt-5 grid gap-3 rounded-2xl bg-stone-100 p-5 sm:grid-cols-2"><input name="invitationId" type="hidden" value={invitationId} /><input className="rounded-xl border border-stone-200 bg-white px-3 py-2.5 text-sm" name="title" placeholder="Event name (e.g. Mehendi)" required /><input className="rounded-xl border border-stone-200 bg-white px-3 py-2.5 text-sm" name="startsAt" required type="datetime-local" /><input className="rounded-xl border border-stone-200 bg-white px-3 py-2.5 text-sm" name="venueName" placeholder="Venue (optional)" /><input className="rounded-xl border border-stone-200 bg-white px-3 py-2.5 text-sm" name="mapUrl" placeholder="Google Maps link (optional)" type="url" /><button className="sm:col-span-2 rounded-xl bg-rose-700 px-4 py-3 text-sm font-semibold text-white">Add event</button></form></section>;
}
