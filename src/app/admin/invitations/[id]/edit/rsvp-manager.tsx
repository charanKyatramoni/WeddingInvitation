import { CheckCircle2, MessageCircle, XCircle } from "lucide-react";

type Rsvp = { id: string; guest_name: string; attendance: "attending" | "declined" | "undecided"; note: string | null; created_at: string };

export function RsvpManager({ rsvps }: { rsvps: Rsvp[] }) {
  return <section className="mt-14 border-t border-stone-200 pt-10"><div className="flex items-center gap-2 text-rose-700"><MessageCircle size={19} /><p className="text-sm font-semibold uppercase tracking-[0.18em]">Guest responses</p></div><h2 className="mt-3 text-2xl font-semibold">RSVP inbox</h2><p className="mt-2 text-sm text-stone-600">Every RSVP and guest message submitted from the public invitation is saved here.</p>{rsvps.length ? <div className="mt-6 grid gap-3">{rsvps.map((rsvp) => <article className="rounded-2xl border border-stone-200 bg-white p-5" key={rsvp.id}><div className="flex items-start justify-between gap-4"><div><p className="font-semibold">{rsvp.guest_name}</p><p className="mt-1 text-sm text-stone-500">{new Intl.DateTimeFormat("en-IN", { dateStyle: "medium", timeStyle: "short" }).format(new Date(rsvp.created_at))}</p></div><span className={`inline-flex items-center gap-1 rounded-full px-2.5 py-1 text-xs font-bold ${rsvp.attendance === "attending" ? "bg-emerald-50 text-emerald-700" : "bg-stone-100 text-stone-600"}`}>{rsvp.attendance === "attending" ? <CheckCircle2 size={13} /> : <XCircle size={13} />}{rsvp.attendance === "attending" ? "Attending" : "Declined"}</span></div>{rsvp.note && <p className="mt-4 border-t border-stone-100 pt-4 text-sm leading-6 text-stone-700">“{rsvp.note}”</p>}</article>)}</div> : <div className="mt-6 rounded-2xl border border-dashed border-stone-300 bg-white p-6 text-sm text-stone-500">No RSVP responses yet. They will appear here once guests submit the form.</div>}</section>;
}
